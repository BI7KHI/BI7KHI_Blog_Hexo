# Nexmoe 主题音乐播放器使用指南

## 功能特性

✅ **已成功集成 APlayer 音乐播放器到您的 Nexmoe 主题！**

### 🎵 支持的功能

1. **全局固定播放器** - 在页面底部显示，跨页面播放
2. **文章内嵌播放器** - 可在特定文章中插入独立播放器
3. **歌词显示** - 支持 LRC 格式歌词同步显示
4. **播放列表** - 支持多首歌曲切换播放
5. **主题适配** - 自动适配 Nexmoe 主题的青色配色方案

## 📁 文件结构

```
source/
├── music/          # 音乐文件存储目录
│   ├── song1.mp3   # 音乐文件
│   ├── song2.mp3
│   └── ...
└── images/         # 音乐封面图片
    ├── music-cover1.jpg
    ├── music-cover2.jpg
    └── ...
```

## 🎛️ 配置说明

### 全局播放器配置

在 `_config.nexmoe.yml` 的 `slotFooter` 部分，您可以修改以下配置：

```javascript
audio: [
  {
    name: '歌曲名称',
    artist: '歌手名称', 
    url: '/music/歌曲文件.mp3',
    cover: '/images/封面图片.jpg',
    lrc: '[00:00.00]歌词内容...'  // 或者 lrc: '/music/歌词文件.lrc'
  }
]
```

### 文章内播放器

在任何 Markdown 文章中插入以下代码：

```html
<div id="article-player-1" class="article-music-player"></div>
<script>
document.addEventListener('DOMContentLoaded', function() {
  const ap = new APlayer({
    container: document.getElementById('article-player-1'),
    autoplay: false,
    theme: '#22d9df',
    audio: [{
      name: '文章专用音乐',
      artist: '歌手',
      url: '/music/article-song.mp3',
      cover: '/images/article-cover.jpg'
    }]
  });
});
</script>
```

## 🎨 自定义样式

播放器已自动适配 Nexmoe 主题的设计风格：

- **主题色**：使用 Nexmoe 的青色主题色 `#22d9df`
- **透明背景**：使用毛玻璃效果
- **响应式设计**：自动适配移动端显示

## 📝 使用建议

### 音乐文件格式
- 推荐使用 **MP3** 格式（兼容性最好）
- 文件大小建议控制在 **10MB** 以内
- 比特率推荐 **128-320kbps**

### 封面图片
- 推荐尺寸：**300x300px** 或 **500x500px**
- 格式：**JPG** 或 **PNG**
- 文件大小：**< 500KB**

### 歌词文件
- 格式：**LRC** 格式
- 编码：**UTF-8**
- 可以在线生成或使用歌词编辑器制作

## 🔧 高级配置

### 自动播放（需谨慎使用）
```javascript
autoplay: true  // 改为 true 开启自动播放（可能被浏览器阻止）
```

### 音量控制
```javascript
volume: 0.5  // 0.0-1.0，默认音量 50%
```

### 播放模式
```javascript
loop: 'all',     // 'all'(列表循环), 'one'(单曲循环), 'none'(不循环)
order: 'random'  // 'list'(顺序播放), 'random'(随机播放)
```

## 🚀 部署注意事项

1. **音乐文件版权**：请确保使用的音乐文件拥有合法的使用权限
2. **CDN 加速**：建议将音乐文件上传到 CDN 以提高加载速度
3. **HTTPS 支持**：确保音乐文件 URL 支持 HTTPS
4. **移动端优化**：考虑移动端流量消耗，可设置默认不自动播放

## 🎯 实际效果

- **全局播放器**：固定在页面底部，不影响正常浏览
- **主题一致性**：与 Nexmoe 主题完美融合
- **用户体验**：支持键盘快捷键、拖拽进度等操作

现在您可以开始上传音乐文件并更新播放列表配置了！

## 示例配置更新

请将以下音乐信息替换为您自己的：

```javascript
audio: [
  {
    name: '您的歌曲名',
    artist: '您的歌手名', 
    url: '/music/your-song.mp3',
    cover: '/images/your-cover.jpg',
    lrc: '/music/your-lyrics.lrc'
  }
]
```
